#include "main.h"

/**
 * _isupper - transform character in uppercase
 * @c: its the variable
 *
 *Description: to the lowercase into uppercase
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	} 
	else
	{
		return (0);
	}
}
