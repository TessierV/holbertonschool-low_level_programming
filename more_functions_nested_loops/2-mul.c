#include "main.h"

/**
 * mul - multiplication
 * @a: its the variable
 * @b: its the variable
 *
 *Description: multiplication
 *Return: the result of the the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
