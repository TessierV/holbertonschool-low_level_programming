#include "main.h"

/**
 * mul - multiplication
 * @a: its the variable
 * @b: its the variable
 *
 *Description: multiplication
 *Return: the result of the the multiplication
 */
void print_numbers(void)
{
	int n = 0;
	for(n = 0; n < 9; n++)
	{
		putchar(n);
		putchar('/n');
	}
}
